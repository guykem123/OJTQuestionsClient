import { SmallLettersBoldNumbersPipe } from './small-letters-bold-numbers.pipe';

describe('SmallLettersBoldNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallLettersBoldNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
