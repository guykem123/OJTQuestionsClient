import { TreeNodeViewDirective } from './tree-node-view.directive';

describe('TreeNodeViewDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeNodeViewDirective();
    expect(directive).toBeTruthy();
  });
});
